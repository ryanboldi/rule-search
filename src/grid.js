class Grid {
    /**
     * 
     * @param {Number} width # of cells in each row of the grid
     * @param {Number} height # of cells in each col of the grid 
     */
    constructor(width, height){
        this.width = width;
        this.height = height;
    }

    draw(gridSize, topLeft, topRight){

    }
}